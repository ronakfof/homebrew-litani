name: Test Kani
on:
  push:
    paths:
      - 'Formula/kani.rb'
  workflow_dispatch:

jobs:
  setup-kani:
    runs-on: macos-10.15
    steps:
      - name: Install Kani
        run: brew install ronakfof/litani/kani
      - name: Test kani
        run: brew test kani
        